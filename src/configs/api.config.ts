class ApiConfig {
    baseURL = 'http://localhost:3000';

    auth = '/auth';
    user = '/user';
    news = '/news';
    images = '/images';
    skins = '/skins';
}

export const apiConfig = new ApiConfig();